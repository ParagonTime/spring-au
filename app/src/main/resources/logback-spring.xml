<configuration>
    <!-- Подключаем базовые настройки Spring Boot 4 -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml" />

    <!-- Настраиваем OTLP Аппендер (Класс БЕЗ .appender. в названии пакета) -->
    <appender name="OTLP" class="io.opentelemetry.instrumentation.logback.v1_0.OpenTelemetryAppender">
    </appender>

    <!-- Оставляем вашу консоль -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="OTLP"/>
    </root>
</configuration>
