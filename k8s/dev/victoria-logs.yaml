apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vl-storage-pvc
  namespace: dev
  labels:
    app: victoria-logs
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: victoria-logs
  namespace: dev
  labels:
    app: victoria-logs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: victoria-logs
  template:
    metadata:
      labels:
        app: victoria-logs
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: victoria-logs
          image: victoriametrics/victoria-logs:v1.30.0
          imagePullPolicy: IfNotPresent
          args:
            - "--storageDataPath=/storage"
            - "--retentionPeriod=1d"
            - "-opentelemetry.http.listenAddr=:9428"
          ports:
            - name: http
              containerPort: 9428
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /health
              port: http
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health
              port: http
              scheme: HTTP
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          volumeMounts:
            - name: storage-volume
              mountPath: /storage
      volumes:
        - name: storage-volume
          persistentVolumeClaim:
            claimName: vl-storage-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: victoria-logs
  namespace: dev
  labels:
    app: victoria-logs
spec:
  selector:
    app: victoria-logs
  ports:
    - name: http
      port: 9428
      targetPort: http
      protocol: TCP
  type: ClusterIP