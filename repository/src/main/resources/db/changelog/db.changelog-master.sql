--liquibase formatted sql

--changeset developer:1
CREATE TABLE users (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    login VARCHAR(255) NOT NULL UNIQUE,
    role VARCHAR(20) NOT NULL,
    email VARCHAR(255) NOT NULL UNIQUE,
    created_at TIMESTAMP NOT NULL,
    user_id UUID NOT NULL UNIQUE
);

--changeset developer:2
CREATE TABLE tasks (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    description TEXT,
    status VARCHAR(50) NOT NULL,
    user_id UUID,
    created_at TIMESTAMP NOT NULL
);

--changeset developer:3
CREATE INDEX idx_tasks_user_id ON tasks(user_id);

--changeset developer:4
CREATE TABLE search_users (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id UUID NOT NULL UNIQUE,
    role VARCHAR(20) NOT NULL,
    email VARCHAR(255) NOT NULL UNIQUE,
    created_at TIMESTAMP NOT NULL
);

--changeset developer:5
CREATE TABLE search_tasks (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    task_id BIGINT NOT NULL,
    title VARCHAR(255) NOT NULL,
    description TEXT,
    status VARCHAR(50) NOT NULL,
    user_id UUID,
    created_at TIMESTAMP NOT NULL
);

--changeset developer:7
CREATE EXTENSION IF NOT EXISTS pg_trgm;

--changeset developer:8
CREATE INDEX IF NOT EXISTS idx_search_users_email_trgm ON search_users USING gin (email gin_trgm_ops);